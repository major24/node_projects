
// TDD with chai
const chai = require('chai');
const assert = require('assert');


describe('When test is triggered', () => {
  it('Then ensure we can run test', () => {
		assert.equal(1, 1);
  });
});
