import { shallowMount } from '@vue/test-utils'
import SumAction from '../src/sum-action'

describe('SumAction', () => {
  // const wrapper = shallowMount(SumAction, {})

  it('renders the correct markup', () => {
    expect(1).toBe(1)
  //   console.log(wrapper.find('sum').html())
  })

})
